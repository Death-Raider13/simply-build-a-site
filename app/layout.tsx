import type React from "react"
import type { Metadata } from "next"
import { Inter } from "next/font/google"
import "./globals.css"
import { ThemeProvider } from "@/components/theme-provider"
import { Toaster } from "@/components/ui/toaster"

const inter = Inter({ subsets: ["latin"] })

export const metadata: Metadata = {
  title: "KNITTED_GOURMET Nigeria - Premium Nigerian Marketplace",
  description:
    "Discover authentic Nigerian confectioneries, handcrafted bags, clothing, and accessories from talented local vendors across Nigeria.",
  keywords: "Nigerian marketplace, confectioneries, handcrafted bags, Nigerian clothing, accessories, local vendors",
    generator: 'v0.dev'
}

export default function RootLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return (
    <html lang="en">
      <body className={inter.className}>
        <ThemeProvider attribute="class" defaultTheme="light" enableSystem disableTransitionOnChange>
          {children}
          <Toaster />
        </ThemeProvider>
      </body>
    </html>
  )
}
